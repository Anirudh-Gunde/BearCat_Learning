<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/bearcat"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bearcat"
        android:supportsRtl="true"
        android:theme="@style/Theme.BearCatLearning"
        tools:targetApi="31">
        <activity
            android:name=".AnimationActivity"

            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".dashboardActivity"></activity>
        <activity android:name=".contentActivity"></activity>
        <activity android:name=".AdminDashboardActivity"></activity>
        <activity android:name=".ContentModificationActivity"></activity>
        <activity android:name=".JavaCourseActivity"></activity>
        <activity android:name=".PdfReaderActivity"></activity>

    </application>

</manifest>